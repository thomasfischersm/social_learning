# Tells Codex to run the tiny bootstrap first
setup_script: ./bootstrap_codex.sh

setup:
  - ./tool/codex_setup.sh

# Optionally tell Codex how to run tests
test:
  - flutter analyze
  - flutter test --coverage
